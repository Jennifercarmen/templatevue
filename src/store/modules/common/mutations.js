/* ============
 * Mutations for the common module
 * ============
 *
 * The mutations that are available on the
 * common module.
 */

export default {
  setEmail (state, payload) {
    state.email = payload
  }
}
